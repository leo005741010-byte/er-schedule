# ============================================================
#  ⚠️ 這個資料夾（桌面）同時是「公開」的 GitHub repo（er-schedule）。
#  桌面上有大量病人明細、個資、Outlook 郵件、照片、程式安裝檔等敏感資料。
#  為避免不小心 `git add .` 把它們推上公開網站，
#  這裡採「白名單」：預設忽略所有東西，只放行排班系統/行事曆的網站檔案。
#
#  👉 日後若要新增一個要公開的網站資料夾，在最下面加一行：
#        !/你的資料夾名稱/
#     或對個別檔案用： git add -f <檔案>
# ============================================================

# 1) 預設先忽略桌面上「所有」檔案與資料夾
/*

# 2) 放行 .gitignore 本身
!/.gitignore

# 3) 放行「急診醫師排班系統」的網站檔案
!/index.html
!/天晟急診排班.html
!/manifest.json
!/sw.js
!/apple-touch-icon.png
!/icon-192.png
!/icon-512.png
!/icon-512-maskable.png

# 4) 放行「我的行事曆」App 整個資料夾
!/我的行事曆/
